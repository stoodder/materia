---
name: correctness
description: Fixture stub of the canonical correctness review angle — a Materia artifact present in a pre-tracking install.
---

Fixture stub. A pre-tracking (`0.1.0`) Materia install already materialized the
`.materia/review-angles/` library, so its presence is a detection signal that
this repo is Materia-enabled — even though no `.materia/project.json` exists yet.
