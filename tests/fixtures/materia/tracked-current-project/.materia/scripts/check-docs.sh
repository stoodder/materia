#!/bin/sh
# Fixture stub — stands in for the shipped scaffold/.materia/scripts/check-docs.sh
# (the binding check:docs gate) at its canonical location. Doctor tests this file's
# PRESENCE (check-docs-sh-present) and LOCATION (check-docs-sh-location); fixtures
# are never executed, so this is a no-op placeholder rather than the real checker.
exit 0
