#!/bin/sh
# Fixture stub — stands in for the shipped scaffold/scripts/check-docs.sh (the
# binding `sh scripts/check-docs.sh` docs gate). Doctor only tests this file's
# PRESENCE (check-docs-sh-present); fixtures are never executed, so this is a
# no-op placeholder rather than the real checker.
exit 0
