# CLAUDE.md

Fixture stub. Marks this tree as a Materia-installed repo (companion to
`MATERIA.md`). Early pre-tracking shape — no `.materia/` at all, and the docs
gate is still the old `scripts/check-docs.mjs` (no `scripts/check-docs.sh`).
