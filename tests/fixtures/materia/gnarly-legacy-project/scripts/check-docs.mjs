#!/usr/bin/env node
// Fixture stub — stands in for the OLD scripts/check-docs.mjs docs checker that
// early dogfood installs shipped before scripts/check-docs.sh replaced it. Its
// presence (and the absence of check-docs.sh) is what makes this fixture model a
// repo that needs the check-docs.sh reconciliation. Never executed by the tests.
process.exit(0)
