<!-- Filled by `/report-bug` or by hand. This is the body only — frontmatter
     lives in the queue contract at docs/bugs/_reports/README.md. Do not embed
     frontmatter here; strip the YAML block before passing this body to any
     consumer. -->

# <title>

_The report's H1 — matches the frontmatter `title`. Replace with a short,
specific defect summary (e.g. "Set-log undo discards the wrong row")._

## Summary

_One sentence describing the defect — inferred from the description._

## Environment

Node 24 · pnpm 9 · Nuxt 4 · Postgres (project defaults). Override if your
environment differs (e.g. a specific Docker image tag, Heroku dyno, or
browser).

## Steps to reproduce

1. _Unknown — please clarify_

## Expected

_Inferred from domain knowledge and the description._

## Actual

_Verbatim from the description._

## Reproducibility

_Intermittent — rate unknown._ (Update to `always` or `intermittent / N in M
attempts` once confirmed.)

## Severity & impact

_Medium._ Mirrors the frontmatter `severity` field; both must agree.

## Affected surface / route / module

_Inferred from description. See docs/surface-map.md for the route/page
inventory. If unresolvable from the description, this field is left as an
open question below._

## Preconditions / data setup

_None known._

## Evidence

_None provided._ (Paste error text, log lines, or screenshot paths here if
available. Repo-root-relative paths are preferred so evidence survives being
copied into a future docs/bugs/&lt;dated-slug&gt;/ run folder.)

## Regression window

_Unknown._ (Provide a last-known-good version, commit SHA, or date if one is
identifiable.)

## Workaround

_None known._

## Open questions

_(Empty unless the description leaves reproduction steps or affected surface
ambiguous — list at most three gaps here.)_
