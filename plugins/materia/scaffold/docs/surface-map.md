# Surface map — every externally reachable surface

<!-- init: adapt the tables to this repo's surface vocabulary (the init
     interview's surface-vocabulary answer), then delete this comment.
     A web app keeps Routes + Pages; a CLI replaces them with a Commands
     table (command · args · what · resource doc); an API/library uses
     Public API (export · signature · what · resource doc); an event
     consumer uses Events.
     Seed empty; docs-sync registers each surface as it ships. This is an
     index — one row per surface, detail stays in the resource docs. -->

## Routes

| Method · Path | What | Auth | Resource doc |
|---|---|---|---|

## Pages

| Path | What | Resource doc |
|---|---|---|
